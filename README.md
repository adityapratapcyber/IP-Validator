# IP-Validator

This project validates IPv4 addresses using Python. It checks whether the entered IP address follows the correct format and ensures that each segment is within the valid range (0–255).

## Features
- Validates IPv4 addresses
- Checks correct format
- Easy to understand code
- Beginner-friendly project

## Example

Input:
192.168.1.1

Output:
Valid IP Address
